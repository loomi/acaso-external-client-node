const decodeToken = (token) => {
  // Do whatever your app need to decode the token identifying the user
  const userData = {
    email: 'xalala@gmail.com',
    first_name: 'Xalala',
    last_name: 'Silva',
  };
  return userData;
};

module.exports = decodeToken;
